%h1 Fashion News
%h2 Editors Newsletter
%table.data
  %tr
    %th Title
    %th Image
    %th Posted on
    %th Type
    %th Active
    %th Actions
  - @editors.each do |post|
    %tr
      %td= link_to post.title, admin_post_path(post)
      %td= image_tag post.media.url(:tiny) rescue ''
      %td= post.created_on
      %td= post.type_post
      %td= post.active
      %td
        = link_to 'Edit', edit_admin_post_path(post)
        = link_to 'Delete', admin_post_path(post), :method => :delete, :confirm => 'Are you sure?'
= link_to 'New post', new_admin_post_path(:type => '1'), :class => 'new-link'
%br
%br
%h2 Chesca Trends
%table.data
  %tr
    %th Title
    %th Image
    %th Posted on
    %th Type
    %th Active
    %th Actions
  - @trends.each do |post|
    %tr
      %td= link_to post.title, admin_post_path(post)
      %td= image_tag post.media.url(:tiny) rescue ''
      %td= post.created_on
      %td= post.type_post
      %td= post.active
      %td
        = link_to 'Edit', edit_admin_post_path(post)
        = link_to 'Delete', admin_post_path(post), :method => :delete, :confirm => 'Are you sure?'
= link_to 'New post', new_admin_post_path(:type => '2'), :class => 'new-link'
%br
%br
%h2 Archived Looks
%table.data
  %tr
    %th Title
    %th Image
    %th Posted on
    %th Type
    %th Active
    %th Actions
  - @looks.each do |post|
    %tr
      %td= link_to post.title, admin_post_path(post)
      %td= image_tag post.media.url(:tiny) rescue ''
      %td= post.created_on
      %td= post.type_post
      %td= post.active
      %td
        = link_to 'Edit', edit_admin_post_path(post)
        = link_to 'Delete', admin_post_path(post), :method => :delete, :confirm => 'Are you sure?'
= link_to 'New post', new_admin_post_path(:type => '3'), :class => 'new-link'


