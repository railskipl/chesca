- cate.children.each do |child|
  - level.times.each do
    &nbsp;&nbsp;
  = link_to child.name,look_path(child), :class =>  'greyText'
  %br
  - if !child.children.empty?
    =render :partial => 'layouts/tree_look', :locals => {:cate => child, :level => level+1}

